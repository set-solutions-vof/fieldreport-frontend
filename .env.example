VITE_API_BASE_URL=http://localhost:8000

# GitHub Packages — classic PAT with read:packages scope.
# Local: loaded by npm scripts via dotenv-cli (see package.json "setup" / "dev").
# CI: add the same token as repository secret NODE_AUTH_TOKEN (Settings → Secrets).
NODE_AUTH_TOKEN=
