VITE_API_BASE_URL=http://localhost:8000

# GitHub Packages — classic PAT with read:packages scope (local dev only).
# CI uses the workflow GITHUB_TOKEN; see README → CI if installs fail with 403.
# Loaded by npm scripts via dotenv-cli (see package.json "setup" / "dev").
NODE_AUTH_TOKEN=
