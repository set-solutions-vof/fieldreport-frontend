VITE_API_BASE_URL=http://localhost:8000

# GitHub Packages — classic PAT with read:packages scope.
# Loaded automatically by npm scripts via dotenv-cli (see package.json "setup" / "dev").
NODE_AUTH_TOKEN=
